# Screenshots

App screenshots used by the root [`README.md`](../../README.md) gallery and release notes.
Captured from a real device, installed as a Hermes Dashboard plugin. All files are lowercase `.png`,
stripped, resized to ≤1080px and palette-compressed (≈90% smaller; the whole folder is ~0.6 MB).

## Current assets

| File | Shown as |
|------|----------|
| `home-screen.png` | Installed on the phone home screen (hero) |
| `chat.png` | Chat — streaming reply (hero + gallery) |
| `activity.png` | Activity — approvals & tasks |
| `projects.png` | Projects — kanban |
| `agents.png` | Agents & workers |
| `profiles_screen.png` | Profiles |
| `system_screen.png` | System status |
| `connection_screen.png` | Sign-in |
| `menu.png` | Navigation drawer |
| `cron.png` | Cron jobs |
| `mobile-tab.png` | Dashboard → Mobile tab (install entry) — _not in the README gallery_ |

## Notes

- These assets are tracked (the `docs/screenshots/` ignore rule was removed) so they render on GitHub.
- A short `demo.gif` (install → chat → approval flow) would strengthen the README later.
