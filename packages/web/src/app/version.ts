// PWA release metadata lives here so UI labels do not borrow the Hermes backend version.
export const HERMES_PWA_RELEASE_VERSION = '0.1.0';

export const HERMES_PWA_VERSION_LABEL = HERMES_PWA_RELEASE_VERSION;
